[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "B",   # flake8-bugbear
    "C4",  # flake8-comprehensions
    "UP",  # pyupgrade
]
ignore = [
    "B008",   # do not perform function calls in default arguments (FastAPI Depends)
    "E501",   # line too long — ruff format handles this
    "UP042",  # StrEnum migration — SQLAlchemy Enum type requires str+Enum pattern
]

[tool.ruff.lint.isort]
known-first-party = ["app"]

[tool.mypy]
# strict=true révèle 147 erreurs de typage pré-existantes dans le repo.
# Décision P0a : démarrer en mode permissif et durcir progressivement.
# TODO P1 (S2) : monter en strict module par module via [[tool.mypy.overrides]].
python_version = "3.12"
strict = false
ignore_missing_imports = true
plugins = ["sqlalchemy.ext.mypy.plugin", "pydantic.mypy"]
# Codes désactivés temporairement (reactiver progressivement en S2)
disable_error_code = [
    "type-arg",        # Missing type arguments for generic type
    "attr-defined",    # SQLAlchemy 2.0 typing strictness
    "no-any-return",   # Wide spectrum, à réactiver après refacto
    "arg-type",        # SQLAlchemy where(...) typing
    "assignment",      # Sequence type variance
    "import-untyped",  # External libs sans stubs
    "index",           # SQLAlchemy Row indexing
    "unused-ignore",   # Pré-existants `# type: ignore` redondants
    "no-untyped-def",
    "no-untyped-call",
    "valid-type",
    "misc",
]

# Modules refondus — empêcher la régression, avec les codes que le projet
# désactive encore globalement.
[[tool.mypy.overrides]]
module = [
    "app.core.middleware",
    "app.core.config",
    "app.core.sentry",
    "app.services.email_service",
    "app.cli.provision_tenant",
]
strict = true

# Détection de doublons : strict RÉEL, sur du code écrit sous ce régime.
#
# `disable_error_code = []` ne suffit pas : il ne rallume pas un code éteint
# globalement plus haut, et le garde annonçait donc « Success » sur une version
# où l'endpoint rendait 500 à chaque requête. `enable_error_code` le rallume.
#
# Ces codes vivent dans leur propre bloc : appliqués aux modules ci-dessus, ils
# réveillent deux erreurs préexistantes de `middleware.py` et rendent la CI
# rouge — ce qui pousserait le prochain pressé à supprimer la ligne plutôt qu'à
# la restreindre.
[[tool.mypy.overrides]]
module = [
    "app.core.names",
    "app.models.user",
    "app.services.duplicates.similarity",
    "app.services.duplicates.detection",
    "app.routers.duplicates",
]
strict = true
enable_error_code = [
    "attr-defined",
    "no-any-return",
    "no-untyped-def",
    "no-untyped-call",
    "type-arg",
    "valid-type",
    "arg-type",
    "assignment",
    "index",
    "misc",
    "unused-ignore",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
python_files = ["test_*.py"]
python_functions = ["test_*"]

[tool.coverage.run]
source = ["app"]
omit = ["app/core/migrations/*", "tests/*"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise NotImplementedError",
    "if TYPE_CHECKING:",
]
