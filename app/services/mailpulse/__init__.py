"""Intégration MailPulse — notifications email + WhatsApp aux parents.

Sous-modules :
- ``client`` : client HTTP async (contacts + messages).
- ``settings_service`` : lecture/écriture de la config MailPulse par tenant.
"""
